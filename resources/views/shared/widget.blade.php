<div class="card">
    <div class="card-body">
        <p> <span class="{{ $icon ?? '' }}"></span> | {{ $title ?? 'Title' }}</p>
        <h2 class="fw-bold fs-2">{{ $data ?? 0 }}</h2>
    </div>
</div>
